---
import { cx, resolveProps } from 'meno-astro';

const __props = resolveProps(Astro, {});
const { class: className } = __props;

const __meno = { acceptsStyles: true };
---
<section class={cx("p-(--s-p) bg-bg", className)}><container class="m-auto block max-w-(--c-s)"><slot /></container></section>
