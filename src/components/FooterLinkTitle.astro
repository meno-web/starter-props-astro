---
import { cx, resolveProps } from 'meno-astro';

const __props = resolveProps(Astro, {
  text: { type: "string", default: "Product" }
});
const { text, class: className } = __props;
---
<span class={cx("mb-4 block text-text text-(length:--s-fs) font-(number:--s-fw) [text-transform:var(--subtitle)] tracking-(--s-ls)", className)}>{text}</span>
